git init
git remote add origin https://github.com/ahmdevlop/DZGPT.git
git add .
git commit -m "Initial commit"
git push -u origin master

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = nextConfig
